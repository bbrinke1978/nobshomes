---
phase: 07-admin-portal
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - src/auth.ts
  - src/middleware.ts
  - src/app/api/auth/[...nextauth]/route.ts
  - src/app/admin/login/page.tsx
  - src/app/admin/page.tsx
  - src/app/admin/layout.tsx
  - package.json
  - next.config.ts
autonomous: false
requirements: [ADMIN-01, ADMIN-02]
must_haves:
  truths:
    - "Visiting /admin redirects unauthenticated users to /admin/login"
    - "Only brian@no-bshomes.com and shawn@no-bshomes.com can sign in"
    - "After Google OAuth, authenticated user lands on /admin dashboard"
    - "Public pages (/, /blog, /gallery) require NO authentication"
  artifacts:
    - path: "src/auth.ts"
      provides: "NextAuth v5 config with Google OAuth, JWT sessions, email allowlist"
      exports: ["auth", "handlers", "signIn", "signOut"]
    - path: "src/middleware.ts"
      provides: "Route protection redirecting unauthenticated /admin/* to /admin/login"
    - path: "src/app/api/auth/[...nextauth]/route.ts"
      provides: "NextAuth route handler"
      exports: ["GET", "POST"]
    - path: "src/app/admin/login/page.tsx"
      provides: "Login page with Google sign-in button"
    - path: "src/app/admin/page.tsx"
      provides: "Admin dashboard with links to gallery, blog, testimonials"
    - path: "src/app/admin/layout.tsx"
      provides: "Admin layout wrapper with nav sidebar/header"
  key_links:
    - from: "src/middleware.ts"
      to: "src/auth.ts"
      via: "import auth"
      pattern: "import.*auth.*from.*@/auth"
    - from: "src/app/api/auth/[...nextauth]/route.ts"
      to: "src/auth.ts"
      via: "import handlers"
      pattern: "import.*handlers.*from.*@/auth"
user_setup:
  - service: google-oauth
    why: "Google OAuth for admin login"
    env_vars:
      - name: AUTH_SECRET
        source: "Generate with: npx auth secret (or openssl rand -base64 33)"
      - name: AUTH_GOOGLE_ID
        source: "Google Cloud Console -> APIs & Services -> Credentials -> OAuth 2.0 Client ID"
      - name: AUTH_GOOGLE_SECRET
        source: "Google Cloud Console -> APIs & Services -> Credentials -> OAuth 2.0 Client Secret"
      - name: AUTH_TRUST_HOST
        source: "Set to 'true' (required for Netlify reverse proxy)"
    dashboard_config:
      - task: "Create OAuth 2.0 Client ID (Web application type)"
        location: "Google Cloud Console -> APIs & Services -> Credentials"
      - task: "Add authorized redirect URI: https://no-bshomes.com/api/auth/callback/google"
        location: "Google Cloud Console -> OAuth Client -> Authorized redirect URIs"
      - task: "Add localhost redirect URI for dev: http://localhost:3000/api/auth/callback/google"
        location: "Google Cloud Console -> OAuth Client -> Authorized redirect URIs"
      - task: "Configure OAuth consent screen (External or Internal if using Workspace)"
        location: "Google Cloud Console -> APIs & Services -> OAuth consent screen"
---

<objective>
Set up NextAuth v5 (Auth.js) with Google OAuth provider, email allowlist, and JWT sessions. Protect all /admin/* routes via middleware. Create login page and admin dashboard landing page.

Purpose: Authentication is required before any admin CRUD pages can be built. This gates access to the admin portal.
Output: Working Google OAuth login flow, route protection, admin layout shell.
</objective>

<execution_context>
@./.claude/get-shit-done/workflows/execute-plan.md
@./.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/research/ADMIN-PORTAL-OPTIONS.md (section 1 — NextAuth v5)

@src/components/Header.tsx (UI patterns — Tailwind styling conventions)
</context>

<tasks>

<task type="auto">
  <name>Task 1: Install NextAuth and create auth config + route handler + middleware</name>
  <files>
    package.json
    src/auth.ts
    src/app/api/auth/[...nextauth]/route.ts
    src/middleware.ts
    next.config.ts
  </files>
  <action>
Install:
```bash
npm install next-auth@beta
```

Create `src/auth.ts`:
- Import NextAuth from "next-auth" and Google from "next-auth/providers/google"
- Define ALLOWED_EMAILS array: ["brian@no-bshomes.com", "shawn@no-bshomes.com"] (admin@ can be added later)
- Export { auth, handlers, signIn, signOut } from NextAuth({...})
- providers: [Google] — Auth.js v5 auto-reads AUTH_GOOGLE_ID and AUTH_GOOGLE_SECRET env vars
- session: { strategy: "jwt" }
- callbacks:
  - signIn({ user }): return ALLOWED_EMAILS.includes(user.email ?? "") — blocks unauthorized emails
  - jwt({ token }): return token (passthrough)
  - session({ session, token }): return session (passthrough)
- pages: { signIn: "/admin/login", error: "/admin/login" }

Create `src/app/api/auth/[...nextauth]/route.ts`:
- Import { handlers } from "@/auth"
- Export const { GET, POST } = handlers

Create `src/middleware.ts` (at project root src/):
- Import { auth } from "@/auth"
- Export default auth((req) => { if (!req.auth && req.nextUrl.pathname.startsWith("/admin") && !req.nextUrl.pathname.startsWith("/admin/login")) { return Response.redirect(new URL("/admin/login", req.nextUrl.origin)) } })
- IMPORTANT: Do NOT redirect /admin/login itself — that causes infinite redirect loop
- Export config = { matcher: ["/admin/:path*"] }

Update `next.config.ts` if needed — Auth.js v5 should work without changes to next.config. Do NOT set AUTH_URL.
  </action>
  <verify>
    <automated>npx tsc --noEmit src/auth.ts src/middleware.ts src/app/api/auth/[...nextauth]/route.ts 2>&1 | head -20</automated>
  </verify>
  <done>
    - next-auth@beta installed
    - src/auth.ts exports auth, handlers, signIn, signOut with Google provider + email allowlist
    - Route handler at /api/auth/[...nextauth] exports GET and POST
    - Middleware protects /admin/* (except /admin/login) by redirecting to /admin/login
  </done>
</task>

<task type="auto">
  <name>Task 2: Create admin login page, dashboard, and layout</name>
  <files>
    src/app/admin/login/page.tsx
    src/app/admin/page.tsx
    src/app/admin/layout.tsx
  </files>
  <action>
Create `src/app/admin/login/page.tsx`:
- Server Component (no "use client")
- Import { signIn } from "@/auth"
- Centered card layout with: No BS Homes admin heading, "Sign in to manage your site" subtext
- Form with action that calls signIn("google", { redirectTo: "/admin" }) — use a server action
- Single button: "Sign in with Google" styled with brand colors (bg-brand-500 text-white rounded-xl)
- Include the No BS Homes stamp logo at top
- Handle error search param: if ?error=AccessDenied show "Your email is not authorized to access admin" in red

Create `src/app/admin/layout.tsx`:
- Server Component
- Import { auth } from "@/auth"
- Get session via await auth()
- Layout with a top navigation bar showing:
  - "No BS Homes Admin" text/logo on left
  - Nav links: Dashboard, Gallery, Blog, Testimonials (as Link components to /admin, /admin/gallery, /admin/blog, /admin/testimonials)
  - User email display + sign out button on right (sign out via server action calling signOut)
- Use Tailwind: bg-slate-900 text-white for admin nav (dark theme to visually distinguish from public site)
- Children rendered below the nav in a padded container
- If no session (user on /admin/login), render children without the admin nav

Create `src/app/admin/page.tsx`:
- Server Component
- Import { auth } from "@/auth"
- Get session, display "Welcome, {session.user.name}" heading
- Dashboard grid with 3 cards linking to:
  - Gallery Management (/admin/gallery) — image icon, "Manage project photos"
  - Blog Posts (/admin/blog) — file icon, "Create and edit blog posts"
  - Testimonials (/admin/testimonials) — message icon, "Manage customer testimonials"
- Use Tailwind card styling consistent with public site (rounded-2xl, shadow, hover state)
- Import icons from lucide-react: Image, FileText, MessageSquare
  </action>
  <verify>
    <automated>npm run build 2>&1 | tail -20</automated>
  </verify>
  <done>
    - /admin/login shows Google sign-in button
    - /admin shows dashboard with 3 management cards
    - Admin layout has dark nav bar with links to all admin sections
    - Build passes successfully
  </done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Verify Google OAuth login flow end-to-end</name>
  <action>
User verifies the authentication flow works correctly with real Google OAuth credentials. This requires creating a Google Cloud OAuth client and setting environment variables, which Claude cannot do.
  </action>
  <what-built>Google OAuth authentication protecting /admin/* routes. Login page at /admin/login, admin dashboard at /admin with navigation to gallery, blog, and testimonials management sections.</what-built>
  <how-to-verify>
1. Set env vars locally: AUTH_SECRET (generate with `npx auth secret`), AUTH_GOOGLE_ID, AUTH_GOOGLE_SECRET, AUTH_TRUST_HOST=true
2. Create Google OAuth Client ID at console.cloud.google.com:
   - APIs & Services -> Credentials -> Create OAuth client ID -> Web application
   - Authorized redirect URIs: http://localhost:3000/api/auth/callback/google
   - OAuth consent screen: add your email as test user
3. Run `npm run dev`
4. Visit http://localhost:3000/admin — should redirect to /admin/login
5. Click "Sign in with Google" — should redirect to Google OAuth
6. Sign in with your @no-bshomes.com email — should land on /admin dashboard
7. Verify dashboard shows 3 cards: Gallery, Blog, Testimonials
8. Try signing in with a non-allowlisted email — should show "not authorized" error
9. Visit http://localhost:3000/ — should load public homepage (no auth required)
10. Set the same env vars on Netlify (Site settings -> Environment variables) for production
  </how-to-verify>
  <verify>User confirms OAuth flow works</verify>
  <done>Google OAuth login works end-to-end for allowlisted emails, rejects non-allowlisted, public site unaffected</done>
  <resume-signal>Type "approved" or describe issues</resume-signal>
</task>

</tasks>

<verification>
1. `npm run build` passes
2. /admin redirects to /admin/login when not authenticated
3. Google OAuth flow completes for allowlisted email
4. Non-allowlisted email is rejected at signIn callback
5. Public pages unaffected (no auth required)
</verification>

<success_criteria>
- NextAuth v5 with Google OAuth fully functional
- Email allowlist restricts access to brian@ and shawn@no-bshomes.com
- Middleware redirects unauthenticated /admin/* to /admin/login
- Admin layout with dark nav bar and management links
- Public site unaffected
</success_criteria>

<output>
After completion, create `.planning/phases/07-admin-portal/07-02-SUMMARY.md`
</output>
